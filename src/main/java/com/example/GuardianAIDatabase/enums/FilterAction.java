package com.example.GuardianAIDatabase.enums;

public enum FilterAction {
    FLAG_ONLY,   // "Flag only — log incidents"
    BLUR,        // "Blur on detection"
    BLOCK        // fully block the content
}
